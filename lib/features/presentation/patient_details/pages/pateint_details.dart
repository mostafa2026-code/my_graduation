import 'package:flutter/material.dart';

class PateintDetails extends StatelessWidget {
  const PateintDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Patient Details")));
  }
}
