enum SymetricalAbnormality { flat, barrel, pectusExcavatum, pectusCarinatum }

enum ChestRegions { subclavicular, mammary, supramammary, infraMammary }

enum ASymetricalAbnormality { retration, bulging }

enum VisiblePulsation {
  epigastricPulsation,
  apicalPulsation,
  ltParasternalPulsation,
  rtParasternalPulsation,
  left2ndSpacePulsation,
  right2ndSpacePulsation,
  suprasternalPulsation,
}

enum BreathingType { abdominal, thoracic,  }

enum VertebralColumn { normal, scoliosis, kyphosis, kyphoscoliosis }

enum SkinChanges { noChanges, scar, pigmentation, nodules, dilatedVeins }

enum UpperChestMovement { normal, reduced }

enum LowerChestMovement { equal, restricted }

enum ChestSide { left, right }

enum VenousFillingDirection { fromAbove, fromBelow }

enum TracheaPosition { central, shiftedRight, shiftedLeft }

enum TVFState { equal, decreased, increased }
